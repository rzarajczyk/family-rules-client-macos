# Manual tests findings


1. Left-click on the tray icon should open the dashboard; there should be no menu after clicking it, just open the window; however, the Dashboard should have the "three dots" icon whish should show this menu (excluding "Show Dashboard" and "All My Devices")

2. Dashboard scrolling is broken - we have a scrollable panel inside a scrollable dashboard; I would like the scrollable app list to finish when the window finished, to prevent showing the main window scrollbar

3. Dashboard app list - move both app timers to the right 

4. Dashboard - remove "Foreground", "Visible", "Sync" and "State" paneld, shring the top blue panel

5. Dashboard should have two tabs under the blue panel: "My device" which should show the current app timers, and "All my devices", which should be essentially the data from "All My Devices" window; "All My Devices" should not be a separate window

6. I need the app to write a log; include in "Diagnostics" the location of the logfile; write all errors in that file

7. Setting the state to "Lock Screen with timeout" shows the countdown on the lock screen - this is wrong, the countdown is supposed to allow user to save their work; make the countdown non-intrusive in top-right screen corner (it should be non-closable, but draggable on the screen)

8. Setting the state to "Lock Screen" (no timeout) does nothing - but the Dashboard shows "The file GCSession doesn't exist"  

9. Setting the state to "Logout" should not literally logout user - it should use "Switch user" feature without asking the user; moreover, this should be an active loop, when user logs in again, the "Switch user" should show again
